{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build",
            "type": "shell",
            "command": "g++ -o passwordManager main.cpp environnement.cpp cryptography.cpp `pkg-config --cflags --libs gtk+-3.0` -lssl -lcrypto ",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
