{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build",
            "type": "shell",
            "command": "g++ main.cpp -o PasswordManager `pkg-config --cflags gtk+-3.0` `pkg-config --libs gtk+-3.0` ",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
